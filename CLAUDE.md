# CLAUDE.md

Les instructions canoniques du dépôt sont dans `AGENTS.md`. Les suivre avec un
contexte minimal : README local pour une tâche locale, documents transversaux
uniquement sur déclencheur, et aucun document de `docs/history/` par défaut.
